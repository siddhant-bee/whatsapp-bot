<!DOCTYPE html>
<html>
<head>
  <title>Customers</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<div class="container">
  <div class="sidebar">
    <h2>📞 Customers</h2>

    <% users.forEach(user => { %>
      <div class="user" onclick="location.href='/chat/<%= user._id %>'">
        <div class="phone no-call">
          <%= user._id %>
        </div>

        <div class="preview">
          <%= user.lastMessage || "No messages yet" %>
        </div>
      </div>
    <% }) %>

  </div>
</div>

</body>
</html>
